你正在维护一个已经完成现代化改造的 Win32 项目 FreePiano。

【当前项目状态（非常重要）】

* 已完成 Unicode 迁移（全部使用 Win32 W API）
* 已实现基于 JSON 的国际化系统（i18n）
* 所有 UI 文本通过 lang_load_string_w() 加载
* 已实现 fallback 机制（当前语言 → 英文 → 硬编码）
* 语言设置已持久化
* 已完成系统级回归测试（核心功能稳定）
* 已支持“运行时切换语言”（无需重启）

⚠️ 注意：
当前阶段 НЕ 需要进行架构重构或大规模改造。

---

## 【本阶段目标】

你的目标是提升以下能力：

1. 可观测性（Observability）
2. 健壮性（Robustness）
3. 可维护性（Maintainability）
4. 调试体验（Debuggability）

所有改动必须是：

* 小步、可控、低风险
* 不破坏现有功能
* 不引入新依赖

---

## 【任务 1：完善日志系统】

实现一个轻量级日志系统（如 fp_log_*）：

建议接口：

* fp_log_info(const wchar_t* fmt, ...)
* fp_log_warn(...)
* fp_log_error(...)

至少覆盖以下场景：

【i18n 相关】

* JSON 加载成功/失败
* 当前语言加载情况
* key 缺失（missing translation）
* fallback 被触发（从当前语言 → 英文）

【系统行为】

* 配置加载
* 关键初始化流程

日志输出位置：

* OutputDebugStringW（必须）
* 可选：输出到文件（logs/freepiano.log）

---

## 【任务 2：增强 i18n 调试能力】

增加调试开关：

#define I18N_DEBUG_SHOW_KEY

开启后：

* 如果找不到翻译：
  显示为 → [IDS_XXX]

* 或：
  在原字符串旁附加标记（如 ⚠）

同时：

* 在 fallback 发生时输出 warning 日志
* 在 key 不存在于任何语言时输出 error 日志

---

## 【任务 3：强化 i18n 健壮性】

改进 language.cpp：

* JSON 加载失败时：

  * 不崩溃
  * 自动回退到硬编码

* 校验 JSON 数据：

  * key 为空
  * value 为空
  * 非法 UTF-8（如果可能）

* 防御性编程：

  * 所有字符串访问必须安全（避免 nullptr）

---

## 【任务 4：完善运行时语言切换后的刷新机制】

虽然已支持运行时切换语言，但请检查并增强：

* 菜单是否完全刷新（包括子菜单）
* 窗口标题是否刷新
* 对话框控件文本是否刷新
* ListView / TreeView / ComboBox 内容是否刷新

如有遗漏：

* 封装统一刷新函数，例如：
  ui_refresh_all_texts()

⚠️ 不允许重构 UI 框架，仅在现有结构上补强

---

## 【任务 5：增加开发者调试工具（可选但推荐）】

实现以下辅助能力：

1. 强制语言环境变量（用于测试）
   例如：
   FREEPIANO_LANG=zh-CN

2. 启动参数：
   --lang=en
   --lang=zh-CN

3. Debug 输出当前语言与资源加载路径

---

## 【任务 6：工程质量改进（小范围）】

* 清理明显重复的 i18n 调用代码
* 统一命名风格（lang_ / tr_ 风格可选其一）
* 减少魔法字符串（hardcoded key）

---

## 【约束条件】

必须遵守：

* ❌ 不重写 i18n 架构
* ❌ 不修改 DSL / 文件格式
* ❌ 不引入第三方库
* ❌ 不大规模重构 UI

---

## 【输出内容】

请输出：

1. 关键 C++ 修改代码（精简且可直接应用）
2. 新增的日志模块代码
3. 对 language.cpp 的增强实现
4. 简要说明每一项改动的目的
