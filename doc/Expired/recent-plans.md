近期计划

### 阶段 1：稳定期

先不要继续大改，做一轮短周期观察。

建议你本机重点验证：
- 四套配置再次全量 Build
- 常用启动路径
- 设置页各项交互
  - MIDI 输入/输出切换
  - VSTi 枚举与打开编辑器
- 导出 WAV
- 键盘映射加载/保存

目标：
- 确认这次现代化 + A 方案收尾没有引入回归

---

### 阶段 2：问题清单化

建议建一个小 backlog，分 3 类：

### P0：必须修

- 会崩溃
- 会构建失败
- 会导致功能不可用

### P1：应修

- 兼容性差
- Release/Debug 行为不一致
- 旧 API 使用但暂不阻塞

### P2：可优化

- 代码风格不统一
- 剩余低价值警告
- 历史命名/结构整理

---

### 阶段 3：功能优先级建议

### 方向 A：继续稳固主程序

优先级最高，适合现在做：
1. WAV 导出回归测试
2. VST 插件扫描与编辑器兼容性测试
3. MIDI 设备热插拔与切换验证
4. 长路径/非 ASCII 路径测试
5. 高 DPI / 多分辨率窗口行为测试

这是最适合当前阶段的路线。

---

### 方向 B：恢复 MP4 导出链

如果后面要做，这是单独支线，不建议直接混到主线稳定期里。

建议步骤：
1. 保持 FREEPIANO_NO_X264_EXPORT 默认启用
2. 单独建分支恢复 MP4 导出
3. 重新选型/验证：
   - x264 的 MSVC 可用构建
   - faac 是否继续保留
   - mp4v2 是否继续修补或替换
4. 最后再决定是否重新开放该功能

---

### 方向 C：工程继续现代化

放在稳定之后再做：
- 统一安全字符串 API
- 统一 Win32/x64 类型转换风格
- 清理剩余警告
- 评估迁移到更新的依赖管理或 CMake

---

### 我建议的实际顺序

### 下一步最推荐

1. 做一轮功能回归
2. 汇总 bug/backlog
3. 优先修主程序稳定性
4. 最后再碰 MP4 导出链

---

### 如果要我接着协助

我建议下一轮直接做这个顺序：

### 下一任务包

- 产出一份 TESTPLAN.md
  - 四配置构建
  - 主流程回归
  - 音频/MIDI/VST/WAV 导出检查项
- 再产出一份 NEXT-STEPS.md
  - P0/P1/P2 backlog
  - MP4 导出支线计划                